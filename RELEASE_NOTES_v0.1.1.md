## v0.1.1 - Repository Operations Upgrade

### Added
- GitHub Actions CI workflow for automated unit testing on push/PR.
- Bug report and feature request issue templates.
- Pull request template with validation checklist.
- `CHANGELOG.md` for versioned change tracking.
- `CONTRIBUTING.md` with setup and contribution rules.
- `ROADMAP.md` with staged product plan through productionization.

### Updated
- README with CI badge and GitHub operations documentation section.

### Validation
- Unit tests pass locally:
  - `python -m unittest discover -s tests -p "test_*.py"`
